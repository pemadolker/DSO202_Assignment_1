apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: dso202-assignment-01
  labels:
    app: task-tracker
    tier: frontend
spec:
  type: ClusterIP 
  selector:
    app: task-tracker
    tier: frontend
  ports:
    - port: 8080
      targetPort: 8080
